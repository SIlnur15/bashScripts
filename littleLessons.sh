#!/bin/bash
fileName="kitty.txt"
touch $(pwd)/$fileName
var1="mysql"
if grep keksql /etc/passwd
then echo "$var1 exists"
elif
echo "writing an another sql engineer to the file $(pwd)/$fileName"
echo "writing is ended with succeed"
then echo "Haidaric" > $(pwd)/$fileName
fi
